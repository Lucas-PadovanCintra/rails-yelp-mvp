<h1>Create Review</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content, label: "Comentário, as: :text" %>
    <%= f.input :rating, collection: 0..5, label: "Nota" %>
    <%= f.submit "Enviar Review", class: "btn btn-primary" %>
<% end %>

<%= link_to "cancelar", restaurant_path(@restaurant), class: "my-3 btn btn-secondary" %>
